{
  "name": "complexob",
  "private": true,
  "scripts": {
    "build": "npm --prefix always-fit---complexo-b ci && npm --prefix always-fit---complexo-b run build"
  },
  "dependencies": {
    "@vercel/node": "^5.1.14"
  }
}
